{
  "name": "shuiguo",
  "version": "0.1.8",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node tools/verify-artifact.mjs && node --test 'tests/*.test.js'"
  }
}
